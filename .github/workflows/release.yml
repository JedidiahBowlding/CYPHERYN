name: Release

on:
  push:
    tags: ["v[0-9]+.[0-9]+.[0-9]+"]

permissions:
  contents: write
  id-token: write
  attestations: write

jobs:
  verify-build-release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with: {fetch-depth: 0}
      - uses: actions/setup-python@v5
        with: {python-version: "3.13", cache: pip, cache-dependency-path: platform/api/pyproject.toml}
      - name: Test, lint, and audit API
        working-directory: platform/api
        run: |
          python -m pip install -e ".[dev]" pip-audit
          python -m pytest --cov=intel_platform --cov-fail-under=60 --cov-report=json:coverage.json
          python ../../scripts/check_critical_coverage.py coverage.json
          python -m ruff check src tests
          python -m pip_audit . --strict
      - uses: actions/setup-node@v4
        with: {node-version: "22.14.0", cache: npm, cache-dependency-path: platform/frontend/package-lock.json}
      - name: Test, build, and audit frontend
        working-directory: platform/frontend
        run: |
          npm ci
          npm run lint
          npm run typecheck
          npm test
          npm audit --audit-level=high
      - name: Build shipped images
        run: |
          python scripts/setup.py --check
          docker compose build api worker frontend taxii
      - name: Scan shipped images
        run: |
          for image in signaltrace-api:latest signaltrace-worker:latest signaltrace-frontend:latest signaltrace-taxii:latest; do
            docker run --rm -v /var/run/docker.sock:/var/run/docker.sock \
              -v "$PWD:/workspace" aquasec/trivy:0.68.2 image \
              --severity HIGH,CRITICAL --ignore-unfixed --ignorefile /workspace/.trivyignore \
              --exit-code 1 "$image"
          done
      - name: Prepare release artifacts
        run: mkdir -p dist
      - name: Generate SBOMs
        uses: anchore/sbom-action@v0.24.2
        with:
          image: signaltrace-api:latest
          output-file: dist/signaltrace-api.spdx.json
      - name: Generate frontend SBOM
        uses: anchore/sbom-action@v0.24.2
        with:
          image: signaltrace-frontend:latest
          output-file: dist/signaltrace-frontend.spdx.json
      - name: Generate worker SBOM
        uses: anchore/sbom-action@v0.24.2
        with:
          image: signaltrace-worker:latest
          output-file: dist/signaltrace-worker.spdx.json
      - name: Generate TAXII SBOM
        uses: anchore/sbom-action@v0.24.2
        with:
          image: signaltrace-taxii:latest
          output-file: dist/signaltrace-taxii.spdx.json
      - name: Package source and checksums
        run: |
          git archive --format=tar.gz --prefix="SignalTrace-${GITHUB_REF_NAME}/" \
            -o "dist/SignalTrace-${GITHUB_REF_NAME}.tar.gz" "$GITHUB_SHA"
          (cd dist && sha256sum * > SHA256SUMS)
      - name: Attest source artifact
        uses: actions/attest-build-provenance@v2
        with:
          subject-path: dist/SignalTrace-*.tar.gz
      - name: Publish GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release create "$GITHUB_REF_NAME" dist/* --verify-tag --generate-notes --title "SignalTrace $GITHUB_REF_NAME"
