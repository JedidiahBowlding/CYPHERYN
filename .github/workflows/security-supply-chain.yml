name: Security and supply chain

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: "17 7 * * 1"
  workflow_dispatch:

permissions:
  contents: read
  security-events: write

jobs:
  dependencies:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
          cache: pip
          cache-dependency-path: platform/api/pyproject.toml
      - name: Audit Python dependencies
        working-directory: platform/api
        run: |
          python -m pip install -e ".[dev]" pip-audit
          python -m pip_audit . --strict
      - uses: actions/setup-node@v4
        with:
          node-version: "22.14.0"
          cache: npm
          cache-dependency-path: platform/frontend/package-lock.json
      - name: Audit locked frontend dependencies
        working-directory: platform/frontend
        run: |
          npm ci
          npm audit --audit-level=high

  secrets:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  container-and-sbom:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Generate non-secret CI environment
        run: python scripts/setup.py --check
      - name: Build core images
        run: docker compose build api worker frontend taxii
      - name: Scan API image
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: signaltrace-api:latest
          format: table
          severity: HIGH,CRITICAL
          exit-code: "1"
          trivyignores: .trivyignore
          ignore-unfixed: true
          scanners: vuln
      - name: Generate API SBOM
        uses: anchore/sbom-action@v0.24.2
        with:
          image: signaltrace-api:latest
          artifact-name: signaltrace-api.spdx.json
